# Copy to apps/web/.env.local and adjust. .env.local is git-ignored.

# Base URL of the Disha API. Local dev default below.
# In prod this points at the Cloud Run URL.
NEXT_PUBLIC_API_URL=http://localhost:8080
