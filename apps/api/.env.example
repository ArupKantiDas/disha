# Copy to apps/api/.env and fill in. .env is git-ignored.

# Port the Express API listens on (Cloud Run injects PORT in prod).
PORT=8080

# Gemini API key (Google AI Studio: https://aistudio.google.com/apikey)
# Needed from Phase 2 onward. Engine + skeleton run without it.
GEMINI_API_KEY=
